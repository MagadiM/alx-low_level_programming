#include <stdio.h>
/**
 * displays all possible combinations of single digit numbers
 * return 0 always
 */
int main(void)
{
int ch;
for (ch = 48; ch <= 57; ch++)
	putchar (n);
}
for (ch = 'a'; ch <= 'z'; ch++)
{
	putchar (ch);
if (ch != 57)
{
	putchar (44);
	putchar (32);
}
	putchar (10);
	return (0);
}
}

