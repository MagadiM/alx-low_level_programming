#include "main.h"

/**
 * _islower -> checks whether the character is lowercase
 * @: a character arguement
 * Return: returns 1 and 0 depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
